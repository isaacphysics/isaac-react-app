apiVersion: v1
kind: Service
metadata:
  name: {{ include "isaac-cs.fullname" . }}
  labels:
    {{- include "isaac-cs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "isaac-cs.selectorLabels" . | nindent 4 }}
