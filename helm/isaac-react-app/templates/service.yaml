apiVersion: v1
kind: Service
metadata:
  name: {{ include "isaac-react-app.fullname" . }}
  labels:
    {{- include "isaac-react-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "isaac-react-app.selectorLabels" . | nindent 4 }}
