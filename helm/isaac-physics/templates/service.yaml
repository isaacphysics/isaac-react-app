apiVersion: v1
kind: Service
metadata:
  name: {{ include "isaac-physics.fullname" . }}
  labels:
    {{- include "isaac-physics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "isaac-physics.selectorLabels" . | nindent 4 }}
